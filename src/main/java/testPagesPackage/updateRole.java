package testPagesPackage;

import org.openqa.selenium.WebDriver;

import testBasePackage.BasePage;

public class updateRole extends BasePage {

    public updateRole(WebDriver driver) {
        super(driver);
    }
}